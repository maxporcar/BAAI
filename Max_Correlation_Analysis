#
# Max, 2025/10/22
# File: Max:Correlation_Analysis.py
# Short description of the task : Apply correlation analysis to business problems
#

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

from scipy import stats

# 1. Input -> 'posem el archiou que volem analitzar'
df = pd.read_csv('Correlation_Analysis_Data.csv')

df.info()
# print(df.iloc[:,1_6].corr)
#correlation, pvalue = stats.pearsonr(df['Marketing_Spend']df['Sales_Revenue'])

# 2. Process
correlation_matrix = df.iloc[:,1:6].corr()

print(correlation_matrix.round(3))
#df[].corr()
#print(df.insull().sum())
#print(df.insull().sum().sum())

# 3. Output
#print('Data loaded successfully!')
#print(f'Data shape: {df.shape}')
#print(f'Correlation: {correlation:.2f}')
#print(f'P value: {pvalue:.4e}')
sns.heatmap(correlation_matrix)
plt.title('Philemon is the most inteligent person in the world')
plt.tight_layout()
plt.show()